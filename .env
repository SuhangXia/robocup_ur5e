# ============================================================================
# RoboCup UR5e 环境变量配置
# ============================================================================

# ROS Master URI（指向 VirtualBox VM 中的 arm_gazebo）
# 根据实际 VM IP 地址修改
ROS_MASTER_URI=http://192.168.56.101:11311

# 本机 IP 地址（需要时手动指定）
# ROS_IP=192.168.56.1

# GPU 设置（针对 NVIDIA RTX 5070 Ti）
NVIDIA_VISIBLE_DEVICES=all
NVIDIA_DRIVER_CAPABILITIES=compute,utility

# Docker Compose 项目名称
COMPOSE_PROJECT_NAME=robocup_ur5e

# 显示服务器（用于 GUI）
DISPLAY=:0

# 日志级别（使用双美元符号避免 docker-compose 展开变量）
ROSCONSOLE_FORMAT=[$${severity}] [$${time}] [$${node}]: $${message}
